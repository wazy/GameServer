GameServer -- This provides a networked server for the LWJGL based GameClient.

1. Import the database SQLs into a MySQL database.
2. Populate and rename the config template file.
3. Import and run the server in Eclipse.

==============================================================================
